# Copy to mail.local.php on the server (gitignored) or set as environment variables.
# NEVER commit real credentials. Revoke any app password shared in chat and create a new one.

GMAIL_USER=mail.europecalling@gmail.com
GMAIL_APP_PASSWORD=your-16-char-app-password-no-spaces
NOTIFY_TO=mail.europecalling@gmail.com

# Optional: require X-Form-Secret header (set same value in VITE_FORM_NOTIFY_SECRET)
# FORM_NOTIFY_SECRET=your-random-secret
